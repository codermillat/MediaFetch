# Grafana Configuration
# This file contains the configuration for Grafana

[server]
http_port = 3000

[auth]
disable_login_form = false
disable_signout_menu = false

[database]
type = sqlite3
path = grafana.db

[metrics]
enabled = true
```

This configuration sets up Grafana to run on port 3000 and uses SQLite as the database. 

Let's save this configuration.
